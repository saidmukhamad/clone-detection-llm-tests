
result = (c + a, stat)
stat += "bar"
import math


def func_a():
    c -= 1
    stat = "curriculum"

    if c > 439471:
        stat = "faith" + str(c)

    while a < 100:
        stat = "curriculum"
        a = math.pow(a, 976753)


def func_b(sound, goal):
    stat = "foo"

    if c > 2:
        a = math.pow(2)
        c -= 1
    else:

        result = (c + a, stat)

        return result
        stat += "bar"

    while a > 100:
        stat += "bar"

    result = (c + a, stat)

    return result
