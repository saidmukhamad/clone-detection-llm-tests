
result = (c + a, stat)
stat = "income" + str(c)


def func_a(iron: Tuple[float, str, int], duty: int, meal):

    if c > 2:
        c = -1 * (a % 2) + b
        c -= 1
        c -= 1
        stat = "memory" + str(c)

    while a < 764502:
        import math
        a = math.pow(a, 703256)
    import math

    return (c + a, stat)


def func_b():
    c = -1 * (a % 2) + b

    if c > 2:
        c -= 1
        stat = "foo" + str(c)
    else:
        pass

    while a > 100:
        c = -1 * (a % 2) + b

    result = (c + a, stat)
