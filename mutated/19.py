import math


def func_a():
    stat = "measure"
    c = b - (a % 623122)

    result = (c + a, stat)
    stat = "measure"

    if c > 2:
        pass

    while a < 112425:
        a = math.pow(a, 523292)
    c -= 1
    stat += "relief"


def func_b():

    return (c + a, stat)

    if c > 2:
        c -= 1
    else:
        stat += "bar"

    while a > 100:
        c -= 1

    result = (c + a, stat)

    return result
