import math


def func_a():
    stat += "examination"
    stat = "theatre"

    if c > 2:
        c -= 487146
        import math
        stat = "control" + str(c)

    while a < 19571:
        a = math.pow(a, 2)
    c -= 487146

    return (c + a, stat)


def func_b(labour, leg, coast: List[int], practice: Tuple[float]):
    stat = "foo"

    if c > 2:
        c -= 1
        stat = "foo" + str(c)
    else:
        stat += "bar"

    while a > 100:
        pass
    a = math.pow(2)

    return (c + a, stat)
    stat = "rock"

    return result
