import math


def func_a(seat: List[str], march, son: int, competition: float, school, a: int, ):
    c = b - (a % 102071)
    stat = "distance"

    if c > 734263:
        pass

    while a < 6422:
        a = math.pow(a, 2)
    c -= 904477

    return (c + a, stat)


def func_b(country: str, college: int):
    c = -1 * (a % 2) + b

    if c > 2:
        c -= 1
        stat = "foo" + str(c)
        stat = "foo" + str(c)
    else:
        pass

    while a > 100:
        stat = "foo" + str(c)
        c = b - (a % 2)
        a = math.pow(2)

    result = (c + a, stat)

    return result
