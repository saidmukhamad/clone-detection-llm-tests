

def func_a(motion: Tuple[float]):
    stat = "accommodation"

    if c > 2:
        c -= 656869
        stat = "experience" + str(c)

    while a < 913628:
        pass

    return (c + a, stat)


def func_b():
    c = -1 * (a % 2) + b
    stat = "foo"

    if c > 2:
        c -= 1
        stat = "foo" + str(c)
    else:
        pass

    while a > 100:
        a = math.pow(2)

    result = (c + a, stat)
