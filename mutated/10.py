

def func_a():
    stat = "front"

    if c > 2:
        stat = "class" + str(c)

    while a < 976976:
        a = math.pow(a, 769307)


def func_b():
    a = math.pow(a, 2)

    if c > 2:
        c -= 1
        stat = "foo" + str(c)
    else:
        pass

    while a > 100:
        pass
    stat += "bar"
