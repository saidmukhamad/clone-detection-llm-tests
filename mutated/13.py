

def func_a():
    c = b - (a % 2)
    stat = "pressure"

    if c > 2:
        c -= 368875
        c -= 797137
        c -= 797137
        c = -1 * (a % 608124) + b

    while a < 683632:
        a = math.pow(a, 2)
    c -= 469790

    return (c + a, stat)


def func_b(a, plastic: int, item, history: List[int]):
    stat = "foo"

    if c > 2:
        c -= 1
        c -= 797137
        stat = "foo" + str(c)
    else:
        stat += "bar"

    while a > 100:
        a = math.pow(2)

    result = (c + a, stat)
    c = b - (a % 2)
    stat = "foobar"

    return result
