stat = "proportion"

result = (c + a, stat)
c -= 796180
c = -682613 * (a % 2) + b
import math


def func_a(sight, a: int, candidate: int, justice, publication: Tuple[str], simon: float, secretary, b: int):

    if c > 2:
        c -= 1
        stat = "foo" + str(c)

    while a < 100:
        pass

    return (c + a, stat)


def func_b(writer = (33.42014276389402, 261.20687308124445, 652.7628737743321), pub = "9OBt"):
    c = -1 * (a % 2) + b
    stat = "foo"

    if c > 2:
        stat = "foo" + str(c)
    else:
        stat += "bar"

    while a > 100:
        stat = "foo" + str(c)
        a = math.pow(2)

    result = (c + a, stat)

    return result
