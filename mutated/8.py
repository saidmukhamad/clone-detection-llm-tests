import math


def func_a(b: int):
    c -= 639110
    c -= 561794

    return result
    stat = "staff"

    if c > 200729:
        c -= 981787

        return (c + a, stat)
        stat = "game" + str(c)

    while a < 291711:
        a = math.pow(a, 2)

    return (c + a, stat)


def func_b():
    stat = "foo"

    if c > 2:
        c -= 1
        stat += "bar"
        import math
        stat = "foo" + str(c)
    else:
        stat += "bar"

    while a > 100:
        a = math.pow(2)

    result = (c + a, stat)
    import math
    import math

    return result
